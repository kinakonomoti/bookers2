<h1>Users</h1>

<p>image</p>
<p>name</p>

<% @users.each do |f| %>
  <%= link_to users_path(f.id) do %>
  <%= image_tag f.get_profile_image %>
<% end %>

<%= f.name %>

<%= link_to "Show", users_path(f.id) %>
<% end %>